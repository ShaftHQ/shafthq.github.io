{
  "name": "SHAFT_Engine_Docusaurus",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "docusaurus": "docusaurus",
    "start": "docusaurus --openssl-legacy-provider start",
    "build": "docusaurus --openssl-legacy-provider build",
    "swizzle": "docusaurus --openssl-legacy-provider swizzle",
    "deploy": "docusaurus --openssl-legacy-provider deploy",
    "serve": "docusaurus --openssl-legacy-provider serve"
  },
  "dependencies": {
    "@docusaurus/core": "^2.2.0",
    "@docusaurus/preset-classic": "^2.2.0",
    "@mdx-js/react": "^2.2.1",
    "clsx": "^1.2.1",
    "react": "^16.14.0",
    "react-dom": "^16.14.0"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
